program {
    /*PROPOSITO: el programa llama al procedimiento Mover_SegúnColor_ con los parametros dirección, **direccion** y color, **color** , mueve el cabezal en la dirección dada tantas celdas como bolitas de color dado hay en la celda actual. 
    PRECONDICIONES: Desde la celda actual debe haber la cantidad de celdas disponibles segun la cantidad de bolitas que hay en la celda actual.
    PARAMETROS:
    *direccion  dirección - direccion a mover.
    *color color - color de bolita que hay en la celda actual.
    */
    Mover_SegúnColor_(Este,Azul)
}
procedure Mover_SegúnColor_(dirección,color) {
    /*PROPOSITO:que mueve el cabezal en la dirección dada tantas celdas como bolitas de color dado hay en la celda actual. 
    PRECONDICIONES: Desde la celda actual debe haber la cantidad de celdas disponibles segun la cantidad de bolitas que hay en la celda actual.
    PARAMETROS:
    *direccion  dirección - direccion a mover.
    *color color - color de bolita que hay en la celda actual.
    */
    repeat(nroBolitas(color)) {
    Mover(dirección)
    }
}