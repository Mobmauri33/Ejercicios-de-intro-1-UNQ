program {
     /*PROPOSITO: el programa llama a un procedimiento con el nombre DibujarLaBandaGloriosaDeAncho_ que dibuja una banda diagonal de color Rojo de cuatro celdas de alto y de tantas celdas de largo como indique el parámetro **ancho** (dibujando hacia arriba y hacia la derecha).  
    PRECONDICIONES: Desde la celda actual debe haber desde la tercer celda al norte de la actual la misma cantidad de celdas hacia el norte que el valor dado en el **ancho** y hacia el este. 
    PARAMETROS:
    *ancho - Numero - celdas de ancho de la banda roja.
    */
    DibujarBandaDeAlto_YAncho_DeColor_(alto, ancho, color)
}

procedure DibujarBandaDeAlto_YAncho_DeColor_(alto, ancho, color) {
    /*PROPOSITO:  dibuja una banda diagonal de color dado **color** de cuatro de tamaño de celdas dadas como indique el parámetro **alto** y **ancho** (dibujando hacia arriba y hacia la derecha).  
    PRECONDICIONES: Desde la celda actual debe haber hacia la direccion norte la cantidad de celdas dadas y el ancho dado hacia el este. 
    PARAMETROS:
    *alto - Numero - celdas de alto de la banda.
    *ancho - Numero - celdas de ancho de la banda.
    *color - Color - color de la banda de bolitas.
    */
    IrALaCeldaInicial()
    repeat (ancho - 1) {
    DibujarLineaRoja()
    PasarALaSiguienteLinea()
    }
    DibujarLineaRoja()
    IrALaCeldaInicial()
}
procedure IrALaCeldaInicial() {
    /*PROPOSITO: mueve el cabezal a la celda del margen inferior izquierdo del tablero.  
    PRECONDICIONES: -. 
    */
    IrAlBorde(Sur)
    IrAlBorde(Oeste)
}

procedure DibujarLinea(alto, color) {
    /*PROPOSITO:  dibuja una linea vertical de bolitas del color dado **color** de la cantidad celdas de alto dadas **alto**.  
    PRECONDICIONES: Desde la celda actual debe haber la cantidad de celdas dadas hacia el norte.
    */
    repeat (alto) {
        Poner(color)
        Mover(Norte)
    }
}

procedure PasarALaSiguienteLinea() {
    /*PROPOSITO: posiciona el cabezal hacia la celda correspondiente para seguir con la siguiente linea roja a dibujar.  
    PRECONDICIONES: Desde la celda actual debe haber una celda hacia la direccion norte y una celda hacia la direccion este.
    */
    repeat (2) {
        Mover(Sur)
    }
    Mover(Este)
}