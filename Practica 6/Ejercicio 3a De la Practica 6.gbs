program {
    /*  PROPOSITO:El programa llama a un procedimiento llamado SacarUnaFicha_SiSePuede(colorDeLaFicha) , que dado el **colorDeLaFicha** que debe sacarse, saque una ficha siempre y cuando la misma esté en la celda. Si no hubiera fichas del color dado, el procedimiento no hace nada. Si hubiera varias fichas, solo debe sacar una.
        PRECONDICIONES:-.
        PARAMETROS:
        * colorDeLaFicha - color - color de la ficha a sacar.
        OBSERVACIONES:Cada ficha se representa con una bolita del color correspondiente.
    */
    SacarUnaFicha_SiSePuede(Rojo)
}
procedure SacarUnaFicha_SiSePuede(colorDeLaFicha) {
    /*  PROPOSITO:Dado el **colorDeLaFicha** que debe sacarse, saque una ficha siempre y cuando la misma esté en la celda. Si no hubiera fichas del color dado, el procedimiento no hace nada. Si hubiera varias fichas, solo debe sacar una.
        PRECONDICIONES:-.
        PARAMETROS:
    *   colorDeLaFicha - color - color de la ficha a sacar.
        OBSERVACIONES:Cada ficha se representa con una bolita del color correspondiente.
    */
    if(hayBolitas(colorDeLaFicha))
    {
        Sacar(colorDeLaFicha)
    }
}