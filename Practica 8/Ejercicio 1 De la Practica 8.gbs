program {
    IrAlBorde_(Este)
}
procedure IrAlBorde_(dirección) {
    /*
    PROPOSITO: lleva al cabezal al borde dado por el parámetro dirección.
    PRECONDICIONES: Tener al menos una celda para poder mover el cabezal sino no se ejecuta.
    PARAMETROS: dirección.
    TIPO: dirección - Dirección hacia donde mueve el cabezal.
    */
    while(puedeMover(dirección))
    {
        Mover(dirección)
    }
}