program {
    SacarTodasLasDeColor_(Rojo)
}
procedure SacarTodasLasDeColor_(color) {
    /*
    PROPOSITO: saca todas las bolitas del color dado por el parámetro color de la celda actual.
    PRECONDICIONES: Tener al menos una bolita del color dado en el parametro sino no se ejecuta.
    PARAMETROS: color.
    TIPO: color - Color de la/s bolita/s a sacar.
    */
    while(hayBolitas(color))
    {
        Sacar(color)
    }
}